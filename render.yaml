services:
  - type: web
    name: rentlogg-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: DB_FILE
        value: ./data/rentlogg.db
